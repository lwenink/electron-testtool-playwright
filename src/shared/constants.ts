export const APP_NAME = 'Playwrapp';
export const APP_VERSION = '0.1.0';

export const WINDOW_CONFIG = {
  DEFAULT_WIDTH: 1200,
  DEFAULT_HEIGHT: 800,
  MIN_WIDTH: 800,
  MIN_HEIGHT: 600,
} as const;

export const BROWSER_TYPES = ['chromium', 'firefox', 'webkit'] as const;

export const LOG_LEVELS = ['info', 'warn', 'error'] as const;